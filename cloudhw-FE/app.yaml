runtime: nodejs22
env: flex
service: default

entrypoint: node server.js

resources:
  cpu: 1
  memory_gb: 1
  disk_size_gb: 10

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 3

env_variables:
  BACKEND_URL: "${BACKEND_URL}"