runtime: nodejs
env: flex
service: default

runtime_config:
  operating_system: "ubuntu24"
  runtime_version: "22"

resources:
  cpu: 1
  memory_gb: 1
  disk_size_gb: 10

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 3

env_variables:
  BACKEND_URL: "${BACKEND_URL}"